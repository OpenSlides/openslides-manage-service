apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    osinstance/service: backendaction
    osinstance/name: {{ .URL }}
  name: backendaction
  namespace: {{ .StackName }}
spec:
  ports:
    - name: "9002"
      port: 9002
      targetPort: 9002
  selector:
    osinstance/service: backendaction
status:
  loadBalancer: {}
